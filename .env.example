# Application Configuration
APP_NAME=manga-reader
NODE_ENV=development
SERVER_PORT=3000

# Data Directory
# Standalone: ./data (relative to project root)
# Container: /app (managed by Docker)
DATA_DIR=./data

# DeepL Translation API
# Get your API key from: https://www.deepl.com/pro-api
# REQUIRED: Translation features will not work without this
DEEPL_API_KEY=your_deepl_api_key_here

# Hugging Face API Token
# Get your token from: https://huggingface.co/settings/tokens
# OPTIONAL: Suppresses unauthenticated warning and enables faster downloads
# HF_TOKEN=your_hf_token_here

# Advanced Configuration (Optional)
# These are automatically derived from DATA_DIR if not specified
# Only override these if you need custom paths

# DB_DIR=./data/db
# MANGA_DIR=./data/manga
# MANGA_OCR_SOCKET=./data/sock/manga-ocr.sock
